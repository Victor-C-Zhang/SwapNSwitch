<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="RedBlueView">
        <attr name="blue" format="boolean" />
        <attr name="ghostcolor" format="integer" />
    </declare-styleable>

    <declare-styleable name="NumberView">
        <attr name="ghost" format="integer" />
    </declare-styleable>

    <declare-styleable name="ArrowView">
        <attr name="startX" format="float" />
        <attr name="startY" format="float" />
        <attr name="endX" format="float" />
        <attr name="endY" format="float" />
    </declare-styleable>

</resources>
